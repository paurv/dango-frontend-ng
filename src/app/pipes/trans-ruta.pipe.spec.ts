import { TransRutaPipe } from './trans-ruta.pipe';

describe('TransRutaPipe', () => {
  it('create an instance', () => {
    const pipe = new TransRutaPipe();
    expect(pipe).toBeTruthy();
  });
});
